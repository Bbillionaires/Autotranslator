<!-- Android 13+ themed icon: same glyph as ic_launcher_foreground, but as a single-color
     silhouette — the OS applies its own tint over whatever alpha this defines, so the
     fillColor value itself is ignored at render time. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FF000000"
        android:pathData="M30,32 H78 A10,10 0 0 1 88,42 V70 A10,10 0 0 1 78,80 H52 L38,92 V80 H30 A10,10 0 0 1 20,70 V42 A10,10 0 0 1 30,32 Z" />
</vector>
