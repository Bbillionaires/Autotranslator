# Add project specific ProGuard rules here.
# Minification is off by default (see app/build.gradle.kts) for this minimal client, so this
# file is currently unused — kept so enabling minification later doesn't require creating it.
